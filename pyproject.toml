[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "asciidocstring"
dynamic = ["version"]
description = "A semantic parser and converter for Python docstrings written in AsciiDoc"
readme = { file = "README.adoc", content-type = "text/plain" }
requires-python = ">=3.14"
license = "Apache-2.0"
authors = [
    { name = "webmaven" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]
dependencies = [
    "asciidoctrine>=0.2.0a2",
    "lark==1.3.1",
]

[project.optional-dependencies]
griffe = ["griffe>=1.0.0"]
test = [
    "pytest",
    "pytest-cov",
    "docutils",
    "griffe>=1.0.0",
]
lint = [
    "ruff",
    "mypy",
]
dev = [
    "pytest",
    "pytest-cov",
    "docutils",
    "griffe>=1.0.0",
    "ruff",
    "mypy",
]


[project.urls]
Homepage = "https://pypi.org/project/asciidocstring/"
Documentation = "https://github.com/webmaven/asciidocstring#readme"
Repository = "https://github.com/webmaven/asciidocstring"
Issues = "https://github.com/webmaven/asciidocstring/issues"
Changelog = "https://github.com/webmaven/asciidocstring/blob/main/CHANGELOG.adoc"

[tool.hatch.version]
path = "src/asciidocstring/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/asciidocstring"]

[tool.ruff]
line-length = 88
target-version = "py313"  # Ruff supports target version up to current stable
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = []

[tool.mypy]
python_version = "3.14"
strict = true
warn_unreachable = true
pretty = true
